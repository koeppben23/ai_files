{
  "name": "ai-files-governance",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  },
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD"
  }
}