name: CI
on:
  push:
    branches: [ main, master ]
jobs:
  governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Placeholder
        run: echo "master.md guard"
        # master.md guard
      
