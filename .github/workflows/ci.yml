name: CI
on:
  push:
    branches: [ main, master ]
jobs:
  governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # Master guard reference for CI kernel guard test
      - name: Master guard reference
        run: echo "master.md guard" 
      - name: Placeholder
        run: echo "master.md guard"
        # master.md guard
      
