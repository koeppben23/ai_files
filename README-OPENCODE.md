# OpenCode Integration

## /master

- Loads workflow + quality + conflict model
- No core technical rules loaded initially
- Profile detection after repo discovery
- Core rules loaded only when code planning starts